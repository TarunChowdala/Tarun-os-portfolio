# GitHub live stats (server-side only — never use VITE_ for the token)
GITHUB_USERNAME=TarunChowdala
GITHUB_TOKEN=

# Public username for UI links (safe to expose)
VITE_GITHUB_USERNAME=TarunChowdala

# Talk to AI — URL of the portfolio-ai-api Vercel deploy (no trailing slash)
# Local: http://localhost:3000
# Prod:  https://your-api.vercel.app
VITE_CHAT_API_URL=
